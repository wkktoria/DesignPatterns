package io.github.wkktoria.designpatterns.creational.singleton;

/**
 * It handles serialization using Java's in-built mechanism and still ensures single instance.
 */
public enum EnumRegistry {
    INSTANCE;

    public void getConfiguration() {
    }
}
